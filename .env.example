# These are BUILD-TIME variables (Vite inlines them into the static bundle).
# Set them in the Railway "frontend" service → Variables tab BEFORE deploying,
# pointing at your backend service's public Railway URL.

VITE_API_URL=https://your-backend.up.railway.app
VITE_WS_URL=wss://your-backend.up.railway.app/ws
